import React from "react";
import styled from "styled-components";

const Container = styled.div`
  margin-top: 100px;
  text-align: center;
  font-size: 4rem;
`;

export const Reference = () => {
  return (
    <Container>
      <p> Ni imaginas, lo que estamos preparando ...</p>
    </Container>
  );
};
